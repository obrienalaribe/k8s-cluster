apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: orderbook-ingress
  annotations:
    kubernetes.io/ingress.class: kong
spec:
  rules:
  - http:
      paths:
      - path: /order/get
        backend:
          serviceName: cea-orderbook-service-svc
          servicePort: 80
